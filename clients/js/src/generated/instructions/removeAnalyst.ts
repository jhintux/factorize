/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAnalystWhitelistPda, findConfigPda } from "../pdas";
import { FACTORIZE_PROGRAM_ADDRESS } from "../programs";

export const REMOVE_ANALYST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  89, 238, 204, 42, 107, 211, 246, 181,
]);

export function getRemoveAnalystDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_ANALYST_DISCRIMINATOR,
  );
}

export type RemoveAnalystInstruction<
  TProgram extends string = typeof FACTORIZE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAnalyst extends string | AccountMeta<string> = string,
  TAccountAnalystWhitelist extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAnalyst extends string
        ? ReadonlyAccount<TAccountAnalyst>
        : TAccountAnalyst,
      TAccountAnalystWhitelist extends string
        ? WritableAccount<TAccountAnalystWhitelist>
        : TAccountAnalystWhitelist,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveAnalystInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RemoveAnalystInstructionDataArgs = {};

export function getRemoveAnalystInstructionDataEncoder(): FixedSizeEncoder<RemoveAnalystInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REMOVE_ANALYST_DISCRIMINATOR }),
  );
}

export function getRemoveAnalystInstructionDataDecoder(): FixedSizeDecoder<RemoveAnalystInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRemoveAnalystInstructionDataCodec(): FixedSizeCodec<
  RemoveAnalystInstructionDataArgs,
  RemoveAnalystInstructionData
> {
  return combineCodec(
    getRemoveAnalystInstructionDataEncoder(),
    getRemoveAnalystInstructionDataDecoder(),
  );
}

export type RemoveAnalystAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountAnalyst extends string = string,
  TAccountAnalystWhitelist extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  analyst: Address<TAccountAnalyst>;
  analystWhitelist?: Address<TAccountAnalystWhitelist>;
};

export async function getRemoveAnalystInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountAnalyst extends string,
  TAccountAnalystWhitelist extends string,
  TProgramAddress extends Address = typeof FACTORIZE_PROGRAM_ADDRESS,
>(
  input: RemoveAnalystAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveAnalystInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FACTORIZE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    analyst: { value: input.analyst ?? null, isWritable: false },
    analystWhitelist: {
      value: input.analystWhitelist ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.analystWhitelist.value) {
    accounts.analystWhitelist.value = await findAnalystWhitelistPda({
      analyst: getAddressFromResolvedInstructionAccount(
        "analyst",
        accounts.analyst.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("analyst", accounts.analyst),
      getAccountMeta("analystWhitelist", accounts.analystWhitelist),
    ],
    data: getRemoveAnalystInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveAnalystInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist
  >);
}

export type RemoveAnalystInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountAnalyst extends string = string,
  TAccountAnalystWhitelist extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  analyst: Address<TAccountAnalyst>;
  analystWhitelist: Address<TAccountAnalystWhitelist>;
};

export function getRemoveAnalystInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountAnalyst extends string,
  TAccountAnalystWhitelist extends string,
  TProgramAddress extends Address = typeof FACTORIZE_PROGRAM_ADDRESS,
>(
  input: RemoveAnalystInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveAnalystInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountAnalyst,
  TAccountAnalystWhitelist
> {
  // Program address.
  const programAddress = config?.programAddress ?? FACTORIZE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    analyst: { value: input.analyst ?? null, isWritable: false },
    analystWhitelist: {
      value: input.analystWhitelist ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("analyst", accounts.analyst),
      getAccountMeta("analystWhitelist", accounts.analystWhitelist),
    ],
    data: getRemoveAnalystInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveAnalystInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist
  >);
}

export type ParsedRemoveAnalystInstruction<
  TProgram extends string = typeof FACTORIZE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    analyst: TAccountMetas[2];
    analystWhitelist: TAccountMetas[3];
  };
  data: RemoveAnalystInstructionData;
};

export function parseRemoveAnalystInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveAnalystInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      analyst: getNextAccount(),
      analystWhitelist: getNextAccount(),
    },
    data: getRemoveAnalystInstructionDataDecoder().decode(instruction.data),
  };
}
