/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { FACTORIZE_PROGRAM_ADDRESS } from "../programs";

export const SYNC_INVOICE_STATUS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([187, 234, 30, 192, 155, 122, 135, 226]);

export function getSyncInvoiceStatusDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_INVOICE_STATUS_DISCRIMINATOR,
  );
}

export type SyncInvoiceStatusInstruction<
  TProgram extends string = typeof FACTORIZE_PROGRAM_ADDRESS,
  TAccountInvoiceVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInvoiceVault extends string
        ? WritableAccount<TAccountInvoiceVault>
        : TAccountInvoiceVault,
      ...TRemainingAccounts,
    ]
  >;

export type SyncInvoiceStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  invoiceId: string;
};

export type SyncInvoiceStatusInstructionDataArgs = { invoiceId: string };

export function getSyncInvoiceStatusInstructionDataEncoder(): Encoder<SyncInvoiceStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["invoiceId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: SYNC_INVOICE_STATUS_DISCRIMINATOR }),
  );
}

export function getSyncInvoiceStatusInstructionDataDecoder(): Decoder<SyncInvoiceStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["invoiceId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getSyncInvoiceStatusInstructionDataCodec(): Codec<
  SyncInvoiceStatusInstructionDataArgs,
  SyncInvoiceStatusInstructionData
> {
  return combineCodec(
    getSyncInvoiceStatusInstructionDataEncoder(),
    getSyncInvoiceStatusInstructionDataDecoder(),
  );
}

export type SyncInvoiceStatusInput<
  TAccountInvoiceVault extends string = string,
> = {
  invoiceVault: Address<TAccountInvoiceVault>;
  invoiceId: SyncInvoiceStatusInstructionDataArgs["invoiceId"];
};

export function getSyncInvoiceStatusInstruction<
  TAccountInvoiceVault extends string,
  TProgramAddress extends Address = typeof FACTORIZE_PROGRAM_ADDRESS,
>(
  input: SyncInvoiceStatusInput<TAccountInvoiceVault>,
  config?: { programAddress?: TProgramAddress },
): SyncInvoiceStatusInstruction<TProgramAddress, TAccountInvoiceVault> {
  // Program address.
  const programAddress = config?.programAddress ?? FACTORIZE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    invoiceVault: { value: input.invoiceVault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("invoiceVault", accounts.invoiceVault)],
    data: getSyncInvoiceStatusInstructionDataEncoder().encode(
      args as SyncInvoiceStatusInstructionDataArgs,
    ),
    programAddress,
  } as SyncInvoiceStatusInstruction<TProgramAddress, TAccountInvoiceVault>);
}

export type ParsedSyncInvoiceStatusInstruction<
  TProgram extends string = typeof FACTORIZE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    invoiceVault: TAccountMetas[0];
  };
  data: SyncInvoiceStatusInstructionData;
};

export function parseSyncInvoiceStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncInvoiceStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { invoiceVault: getNextAccount() },
    data: getSyncInvoiceStatusInstructionDataDecoder().decode(instruction.data),
  };
}
