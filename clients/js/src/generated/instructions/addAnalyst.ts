/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAnalystWhitelistPda, findConfigPda } from "../pdas";
import { FACTORIZE_PROGRAM_ADDRESS } from "../programs";

export const ADD_ANALYST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  72, 78, 214, 178, 35, 87, 74, 149,
]);

export function getAddAnalystDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_ANALYST_DISCRIMINATOR);
}

export type AddAnalystInstruction<
  TProgram extends string = typeof FACTORIZE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAnalyst extends string | AccountMeta<string> = string,
  TAccountAnalystWhitelist extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAnalyst extends string
        ? ReadonlyAccount<TAccountAnalyst>
        : TAccountAnalyst,
      TAccountAnalystWhitelist extends string
        ? WritableAccount<TAccountAnalystWhitelist>
        : TAccountAnalystWhitelist,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddAnalystInstructionData = { discriminator: ReadonlyUint8Array };

export type AddAnalystInstructionDataArgs = {};

export function getAddAnalystInstructionDataEncoder(): FixedSizeEncoder<AddAnalystInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADD_ANALYST_DISCRIMINATOR }),
  );
}

export function getAddAnalystInstructionDataDecoder(): FixedSizeDecoder<AddAnalystInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAddAnalystInstructionDataCodec(): FixedSizeCodec<
  AddAnalystInstructionDataArgs,
  AddAnalystInstructionData
> {
  return combineCodec(
    getAddAnalystInstructionDataEncoder(),
    getAddAnalystInstructionDataDecoder(),
  );
}

export type AddAnalystAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountAnalyst extends string = string,
  TAccountAnalystWhitelist extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  analyst: Address<TAccountAnalyst>;
  analystWhitelist?: Address<TAccountAnalystWhitelist>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAddAnalystInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountAnalyst extends string,
  TAccountAnalystWhitelist extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FACTORIZE_PROGRAM_ADDRESS,
>(
  input: AddAnalystAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddAnalystInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FACTORIZE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    analyst: { value: input.analyst ?? null, isWritable: false },
    analystWhitelist: {
      value: input.analystWhitelist ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.analystWhitelist.value) {
    accounts.analystWhitelist.value = await findAnalystWhitelistPda({
      analyst: getAddressFromResolvedInstructionAccount(
        "analyst",
        accounts.analyst.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("analyst", accounts.analyst),
      getAccountMeta("analystWhitelist", accounts.analystWhitelist),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddAnalystInstructionDataEncoder().encode({}),
    programAddress,
  } as AddAnalystInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist,
    TAccountSystemProgram
  >);
}

export type AddAnalystInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountAnalyst extends string = string,
  TAccountAnalystWhitelist extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  analyst: Address<TAccountAnalyst>;
  analystWhitelist: Address<TAccountAnalystWhitelist>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAddAnalystInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountAnalyst extends string,
  TAccountAnalystWhitelist extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FACTORIZE_PROGRAM_ADDRESS,
>(
  input: AddAnalystInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddAnalystInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountAnalyst,
  TAccountAnalystWhitelist,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FACTORIZE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    analyst: { value: input.analyst ?? null, isWritable: false },
    analystWhitelist: {
      value: input.analystWhitelist ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("analyst", accounts.analyst),
      getAccountMeta("analystWhitelist", accounts.analystWhitelist),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddAnalystInstructionDataEncoder().encode({}),
    programAddress,
  } as AddAnalystInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountAnalyst,
    TAccountAnalystWhitelist,
    TAccountSystemProgram
  >);
}

export type ParsedAddAnalystInstruction<
  TProgram extends string = typeof FACTORIZE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    analyst: TAccountMetas[2];
    analystWhitelist: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: AddAnalystInstructionData;
};

export function parseAddAnalystInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddAnalystInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      analyst: getNextAccount(),
      analystWhitelist: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddAnalystInstructionDataDecoder().decode(instruction.data),
  };
}
